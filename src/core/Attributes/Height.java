package core.Attributes;

/**
 * The Height enum represents the height attribute for Quarto game pieces.
 * It implements the Attribute interface.
 */
public enum Height implements Attribute {
    TALL, 
    SHORT;
}