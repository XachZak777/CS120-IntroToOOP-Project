package core.attributes;

/**
 * The Color enum represents the color attribute for Quarto game pieces.
 * It implements the Attribute interface.
 */
public enum Color implements Attribute {
    DARK, 
    LIGHT;
}